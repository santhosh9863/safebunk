name: safebunk_shared
description: Shared models, services, providers, and utilities for PULSE apps
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  riverpod: ^2.5.1

dev_dependencies:
  test: ^1.25.0
